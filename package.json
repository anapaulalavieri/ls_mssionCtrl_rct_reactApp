{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@sap/approuter": "^10.5.1",
    "@sap/html5-repo-mock": "^2.1.0"
  },
  "scripts": {
    "build-DubaiExpoIframe": "cd DubaiExpoIframe && if [ ! -d ${workspaceFolder}/iframereact/DubaiExpoIframe/node_modules/react-scripts ] && [ ! -d ${workspaceFolder}/iframereact/node_modules/react-scripts ]; then npm i; fi && npm run build"
  }
}
